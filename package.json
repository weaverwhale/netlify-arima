{
  "name": "tw-netlify-arima",
  "version": "0.1.0",
  "scripts": {
    "start": "yarn && yarn dev",
    "dev": "netlify dev",
    "deploy": "netlify deploy"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0",
    "arima": "^0.2.5",
    "moving-averages": "^4.0.6"
  },
  "devDependencies": {
    "netlify-cli": "^15.1.1"
  }
}
